---
interface Step {
  title: string;
  purpose: string;
  technical: string;
}

interface Props {
  steps: readonly Step[];
  label?: string;
  heading?: string;
  description?: string;
}

const {
  steps,
  label = "Architecture",
  heading = "How the system is structured",
  description = "Each step shows the job it does and the technical decision behind it.",
} = Astro.props;
---

<figure
  class="mt-10 border-t border-slate-200 pt-8 dark:border-slate-800 sm:mt-12 sm:pt-10"
  aria-labelledby="system-path-heading"
>
  <figcaption>
    <p class="font-mono text-xs text-slate-500 dark:text-slate-400">{label}</p>
    <h2
      id="system-path-heading"
      class="scroll-mt-24 mt-2 text-2xl font-semibold tracking-tight text-slate-950 dark:text-white sm:text-3xl"
    >
      {heading}
    </h2>
    <p class="mt-3 max-w-2xl text-base leading-7 text-slate-600 dark:text-slate-400">
      {description}
    </p>
  </figcaption>

  <ol class="mt-6 border-y border-slate-200 dark:border-slate-800 sm:mt-8">
    {
      steps.map((step, index) => (
        <li class="grid grid-cols-[2rem_1fr] gap-x-3 gap-y-1 border-t border-slate-200 py-5 first:border-t-0 sm:grid-cols-[3rem_13rem_1fr] sm:gap-6 sm:py-6 dark:border-slate-800">
          <span class="font-mono text-xs leading-7 text-slate-500 dark:text-slate-400 sm:col-auto">
            {String(index + 1).padStart(2, "0")}
          </span>
          <h3 class="col-start-2 text-base font-semibold leading-7 text-slate-950 dark:text-white sm:col-auto">
            {step.title}
          </h3>
          <div class="col-start-2 sm:col-auto">
            <p class="text-sm leading-7 text-slate-600 dark:text-slate-400">
              {step.purpose}
            </p>
            <details class="group mt-2">
              <summary class="inline-flex min-h-11 cursor-pointer items-center py-2 text-sm font-medium text-slate-800 underline decoration-slate-300 underline-offset-4 hover:text-blue-700 focus-visible:rounded-sm focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-600 dark:text-slate-200 dark:decoration-slate-700 dark:hover:text-blue-400">
                Implementation details
              </summary>
              <p class="mt-3 border-l-2 border-blue-600 pl-4 text-sm leading-7 text-slate-600 dark:text-slate-400">
                {step.technical}
              </p>
            </details>
          </div>
        </li>
      ))
    }
  </ol>
</figure>
